import React from "react";
import { SignUpForm } from "../components/signUpForm";

export const SignUp = () => {
    return (
        <main className="route-signup">
            <SignUpForm />
        </main>
    );
};
